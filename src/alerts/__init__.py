# src/alerts/__init__.py
# The alerts package watches the data for anomalies and
# notifies the user when thresholds are exceeded:
#   detector.py  — detects price spikes and drops
#   notifier.py  — sends email alerts via Gmail SMTP
