# src/database/__init__.py
# The database package manages everything related to PostgreSQL:
#   connection.py — creates and manages the connection pool
#   models.py     — defines the database tables as Python classes (SQLAlchemy ORM)
