# src/monitoring/__init__.py
# The monitoring package exposes Prometheus metrics from the app:
#   metrics.py — defines counters and gauges that track
#                pipeline health (run counts, error rates, latency)
